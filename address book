#include <iostream>
#include <string>
#include<cstring>
#include<stdlib.h>
#include<sstream>
using namespace std;
class person
{
	//data
	private:char id[30];
	char fname[30];
	char lname[30];
	char fullname[40];
	char eaddress[30];
	char address[30];
	char contactNumber[10];
	char buffer[30];
	char pupularity[3];
	char id_list[100][3];
	char rec_index_list[300];
	int count;
	//function(operation)
	public:
		person();
		void input();
		void pack();
		void write();
		void create_list();
		string searchName (char name[40]);
		int  searchContract(char contract[10]);
		void sort ();
		
};
  person::person()
{
	int i=0;
	string s;
 stringstream ss;
ss<<i;
s=ss.str();
strcpy(pupularity,s.c_str());
	cout<<pupularity;
	
}

void person::input()
{
	cout<<"------------Enter your information--------------\n";
	cout<<"Enter first name:";
	cin.getline(fname,30);
	cout<<"\nEnter last name:";
	cin.getline(lname,30);
	cout<<"\nEnter contract number ""include key number( ex:040-2341)"":";	
	cin.getline(contactNumber,30);
	cout<<"\nEnter address:";
	cin.getline(address,30);
	cout<<"\nEnter E-address:";
	cin.getline(eaddress,30);
	strcpy(fullname,strcat(fname,lname));
	
}
void person::pack()
{
	strcpy(buffer,fullname);
	strcat(buffer,"|");	
	strcpy(buffer,address);
	strcat(buffer,"|");
	strcpy(buffer,eaddress);
	strcat(buffer,"|");
	strcpy(buffer,contactNumber);
	strcat(buffer,"|");
	strcpy(buffer,pupularity);
	strcat(buffer,"#");
   

}
string convert(char con[3])
{
	int i=atoi(con);
	i++;
	string s;
 stringstream ss;
ss<<i;
s=ss.str();
/*
strcpy(sp,convert(sp).c_str()); to convert it
*/
return s;
}

int main(int argc, char** argv) {

 person o;
 
	
}
